{
  "name": "slack-random-pick",
  "version": "1.0.0",
  "description": "Pick random user at slack reply.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/inatomitk/slack-random-pick"
  },
  "author": "inatomitk",
  "license": "ISC",
  "dependencies": {
    "@slack/events-api": "^3.0.0",
    "@slack/web-api": "^6.1.0",
    "dotenv": "^9.0.1"
  },
  "devDependencies": {
    "serverless": "^2.40.0"
  }
}
